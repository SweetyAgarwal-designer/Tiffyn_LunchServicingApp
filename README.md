
  # Generate app from document

  This is a code bundle for Generate app from document. The original project is available at https://www.figma.com/design/Tb66zzjjIeA6dvmMDjZloF/Generate-app-from-document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  